{
  "name": "minecraft-adventure",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:game": "vite build",
    "build:editor": "vite build --config vite.config.editor.ts",
    "build:world-editor": "vite build --config vite.config.world-editor.ts",
    "build": "npm run build:game && npm run build:editor && npm run build:world-editor",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}